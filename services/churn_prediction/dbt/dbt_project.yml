name: 'adplatform_batch'
version: '1.0.0'
config-version: 2
profile: 'adplatform_batch'
model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  adplatform_batch:
    staging:
      +materialized: view
    marts:
      +materialized: table
